const symbols = {
  MAX_SIZE   : Symbol(),
  TASK_QUEUE : Symbol(),
  WAITING    : Symbol(),
  PIDS       : Symbol(),
  RESOLVER   : Symbol(),
  REJECTER   : Symbol(),
  KILLSIGINT : Symbol(),
  TIMER      : Symbol(),
  HANDLER    : Symbol()
};

export default symbols;
